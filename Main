from PyQt5.QtWidgets import (
    QApplication, QMainWindow)
from PyQt5.QtGui import QPixmap, QImage
import sys
from qt_material import apply_stylesheet
from UI import Ui_MainWindow
from Image import Image
import numpy as np

class MainWindow(QMainWindow, Ui_MainWindow):
    def __init__(self):
        super().__init__()
        self.setupUi(self)

    def change_ft_component(self, image, ft_label):
        combo_box = self.sender()
        if combo_box is not None:
            if combo_box.currentText() == "Magnitude":
                magnitude_8bit = self.normalize_to_8bit(image.magnitude_log)
                mag_pixmap = self.array_to_pixmap(magnitude_8bit)
                ft_label.setPixmap(mag_pixmap)

            if combo_box.currentText() == "Phase":
                phase_8bit = self.normalize_to_8bit(image.phase_spectrum)
                phase_pixmap = self.array_to_pixmap(phase_8bit)
                ft_label.setPixmap(phase_pixmap)

            if combo_box.currentText() == "Real":
                real_8bit = self.normalize_to_8bit(image.real_component)
                real_pixmap = self.array_to_pixmap(real_8bit)
                ft_label.setPixmap(real_pixmap)

            if combo_box.currentText() == "Imaginary":
                imaginary_8bit = self.normalize_to_8bit(image.imaginary_component)
                imaginary_pixmap = self.array_to_pixmap(imaginary_8bit)
                ft_label.setPixmap(imaginary_pixmap)
    def mix_images(self, images):
        # Cancel any ongoing operation
        if hasattr(self, "worker") and self.worker.isRunning():
            self.worker.cancel()
            self.worker.wait()

        # Create a new worker thread
        self.worker = ImageMixingWorker(images)
        self.worker.progress.connect(self.update_progress_bar)
        self.worker.result_ready.connect(self.display_mixed_image)

        # Start the worker
        self.worker.start()

    def update_progress_bar(self, value):
        self.ui.progressBar.setValue(value)  # Update the progress bar

    def display_mixed_image(self, mixed_image):
        mixed_8bit = self.normalize_to_8bit(mixed_image)
        mixed_pixmap = self.array_to_pixmap(mixed_8bit)
        self.output_1_label.setPixmap(mixed_pixmap)
        self.ui.progressBar.setValue(0)  # Reset progress bar

    # def mix_images(self, images):
    #         self.output_1_image.magnitude_spectrum  = np.zeros_like(self.images[0].image.magnitude_spectrum, dtype=np.float64)
    #         self.output_1_image.phase_spectrum = np.zeros_like(self.images[0].image.phase_spectrum, dtype=np.float64)
    #         self.output_1_image.real_component = np.zeros_like(self.images[0].image.real_component, dtype=np.float64)
    #         self.output_1_image.imaginary_component = np.zeros_like(self.images[0].image.imaginary_component, dtype=np.float64)
            
    #         for image in images:
    #             if image is not None:
    #                 self.output_1_image.magnitude_spectrum = self.output_1_image.magnitude_spectrum  + image.image.magnitude_spectrum*image.magnitude_real_slider.value()/100
    #                 self.output_1_image.phase_spectrum  = self.output_1_image.phase_spectrum  + image.image.phase_spectrum*image.phase_imaginary_slider.value()/100
    #                 self.output_1_image.real_component = self.output_1_image.real_component  + image.image.real_component*image.magnitude_real_slider.value()/100
    #                 self.output_1_image.imaginary_component  = self.output_1_image.imaginary_component  + image.image.imaginary_component*image.phase_imaginary_slider.value()/100

    #         # self.output_1_image.ft_shifted = self.output_1_image.real_component  + 1j * self.output_1_image.imaginary_component
    #         self.output_1_image.ft_shifted = self.output_1_image.magnitude_spectrum/4  *  np.exp(1j * self.output_1_image.phase_spectrum/4)
    #         self.output_1_image.reconstruct_image()
    #         mixed_8bit = self.normalize_to_8bit(self.output_1_image.image)
    #         mixed_pixmap = self.array_to_pixmap(mixed_8bit)
    #         self.output_1_label.setPixmap(mixed_pixmap)

    def normalize_to_8bit(self, array):
        norm = (255 * (array - array.min()) / (array.max() - array.min())).astype(np.uint8)
        return norm
    
    def array_to_pixmap(self, array):
        # Ensure the array is in the right dtype
        if array.dtype != np.uint8:
            raise ValueError("Array must be of type uint8.")

        height, width = array.shape
        bytes_per_line = width
        # Convert the array to bytes
        image_data = array.tobytes()
        # Create a QImage
        qimage = QImage(image_data, width, height, bytes_per_line, QImage.Format_Grayscale8)
        # Convert the QImage to QPixmap
        return QPixmap.fromImage(qimage)
    
def main():
    app = QApplication(sys.argv)
    apply_stylesheet(app, "dark_medical.xml")
    window = MainWindow()
    window.show()
    sys.exit(app.exec_())

if __name__ == "__main__":
    main()
